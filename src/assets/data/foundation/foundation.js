import guidingCategories from './guidingCategories'
import { items, itemTypes } from './rewards'
import accountRoles from './accountRoles'
import rewardConditions from './rewardConditions'

const weekendConditionIds = [
    'session-weekend-1',
    'session-weekend-2',
    'session-weekend-3',
]

export {
    guidingCategories,
    itemTypes,
    items,
    accountRoles,
    rewardConditions,
    weekendConditionIds,
}

export default {
    guidingCategories,
    itemTypes,
    items,
    accountRoles,
    rewardConditions,
    weekendConditionIds,
}